extends ../layouts/basicLayout
block content
    h1.
        Group List
    ul
        each group, i in groups
            li 
                a(href="/viewGroup?groupId=#{group._id}") #{group.name}
                |&nbsp;-&nbsp;
                =group.description
                |&nbsp;-&nbsp;
                =group.administrator